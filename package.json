{
  "name": "views",
  "version": "0.1.0",
  "description": "Demonstrate multiple view implementations",
  "main": "index.js",
  "author": "cwellsx <cwellsx@gmail.com>",
  "license": "SEE LICENSE IN LICENSE.md",
  "private": true,
  "devDependencies": {
    "lerna": "^3.20.2"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "install": "lerna bootstrap",
    "clean": "lerna run clean && rimraf node_modules",
    "test": "lerna run test",
    "build": "lerna run build",
    "unbuild": "lerna run unbuild",
    "watch-lib": "lerna run --parallel --ignore ui* watch",
    "watch-ui": "lerna run --scope ui-react watch",
    "start": "lerna run build --ignore ui* && lerna run --parallel watch",
    "watch": "lerna run --parallel watch"
  }
}
