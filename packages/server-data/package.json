{
  "name": "server-data",
  "version": "0.1.0",
  "description": "Mock data created by prebuild-data for the server",
  "main": "./lib/images.js",
  "types": "./lib/images.d.ts",
  "private": true,
  "dependencies": {
    "shared-lib": "0.1.0"
  },
  "devDependencies": {
    "typescript": "~3.7.2"
  },
  "files": [
    "lib",
    "json"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -p tsconfig.json --watch",
    "clean": "rimraf node_modules",
    "unbuild": "rimraf lib && rimraf src && rimraf json"
  }
}
